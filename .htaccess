RewriteEngine On

# Allow direct access to existing files and directories (including images, css, js)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route /admin to admin.html
RewriteRule ^admin/?$ admin.html [L]
